generator client {
  provider = "prisma-client-js"
  output   = "../../../../node_modules/.prisma/skillforge-client" 
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL_SKILLFORGE")
}